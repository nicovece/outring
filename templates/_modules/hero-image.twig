{#
    Hero Image Module

    Parameters:
    - image (required): Asset object
    - caption (optional): String - custom caption text

    Usage:
    {% if entry.featuredImage|length %}
      {% include '_modules/hero-image' with {
        image: entry.featuredImage.one(),
        caption: entry.featuredImageCaption
      } only %}
    {% endif %}
  #}

<figure class="mb-12">
	<img src="{{ image.url }}" alt="{{ image.title }}" class="w-full" />
	{% if caption %}
		<figcaption class="text-gray-600 mt-2 text-sm">
			{{ caption }}
		</figcaption>
	{% endif %}
</figure>
