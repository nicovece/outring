{#
  Experience Text Section Rich Text Styling
  Applies component classes to CKEditor content for experience text sections
  Usage: {% include '_partials/experience-blocks/experience-text' with { text: block.text } only %}
#}

{{ text|retcon([["removeEmpty", "body > p, body > h1, body > h2"]])|raw }}
